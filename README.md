# XML Validator App


## Setup Backend
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --port 8000


## Setup Frontend
cd frontend
npm install
npm run dev


Open the frontend URL (usually http://localhost:5173) in your browser.


Drag & drop XML/XSD files or paste content into editors and validate.